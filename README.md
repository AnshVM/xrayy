
# xrayy

## Setup

1. Install dependencies:
   ```sh
   npm install
   ```

2. Create a `.env` file in the project root:
   ```env
   MONGODB_URI=mongodb://localhost:27017/xrayy
   ```

## Scripts

- `npm run build` – Compile TypeScript to JavaScript (`dist/`)
- `npm start` – Build and run the compiled code
